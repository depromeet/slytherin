spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bobeat-test
    username: test
    password: bobeat1!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      dialect: org.hibernate.dialect.PostgreSQLDialect